"use strict";

const getUsersWithFriend(users, friendName) => {

    const userWithFriend = users.filter(user => 
        
        if(user.name.includesOf(friendName)) {
        
        }
        

    )

}